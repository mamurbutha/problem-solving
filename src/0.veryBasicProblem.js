const objectMutation = (inputObj, key, newKeyValue) => null;
const copyObject = (inputObj) => {
    const copyed = inputObj;
    return copyed;
}
module.exports = {
    objectMutation,
    copyObject
};